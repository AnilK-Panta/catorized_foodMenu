const Menu = [
  {
    id: 1,
    image:
      "https://images.unsplash.com/photo-1518568740560-333139a27e72?ixid=MnwxMjA3fDB8MHxzZWFyY2h8MXx8bG92ZSUyMHN0b3J5fGVufDB8fDB8fA%3D%3D&ixlib=rb-1.2.1&w=1000&q=80",
    name: "Anil K. Panta",
    Category: "Lunch",
    description:
      "The example above is fine if the web page only contains two The example above is fine if the web page only contains twoThe example above is fine if the web page only contains twoThe example above is fine if the web page only contains twoThe example above is fine if the web page only contains two",
  },
  {
    id: 2,
    image:
      "https://www.shaadidukaan.com/vogue/wp-content/uploads/2019/08/hug-kiss-images.jpg",
    name: "Mr. Nobody",
    Category: "Breakfast",
    description:
      "The example above is fine if the web page only contains two The example above is fine if the web page only contains twoThe example above is fine if the web page only contains twoThe example above is fine if the web page only contains twoThe example above is fine if the web page only contains two",
  },
  {
    id: 3,
    image:
      "https://thumbs.dreamstime.com/b/environment-earth-day-hands-trees-growing-seedlings-bokeh-green-background-female-hand-holding-tree-nature-field-gra-130247647.jpg",
    name: "Jack",
    Category: "Lunch",
    description:
      "The example above is fine if the web page only contains two The example above is fine if the web page only contains twoThe example above is fine if the web page only contains twoThe example above is fine if the web page only contains twoThe example above is fine if the web page only contains two",
  },
  {
    id: 4,
    image:
      "https://image.shutterstock.com/image-photo/patan-ancient-city-kathmandu-valley-260nw-1137140381.jpg",
    name: "Bassi",
    Category: "Dinner",
    description:
      "The example above is fine if the web page only contains two The example above is fine if the web page only contains twoThe example above is The example above is fine if the web page only contains two The example above is fine if the web page only con fine if the web page only contains twoThe example above is fine if the web page only contains twoThe example above is fine if the web page only contains two",
  },
  {
    id: 5,
    image:
      "https://image.shutterstock.com/image-photo/patan-ancient-city-kathmandu-valley-260nw-1137140381.jpg",
    name: "Bassi",
    Category: "Dinner",
    description:
      "The example above is fine if the web page only contains two The example above is fine if the web page only contains twoThe example above is The example above is fine if the web page only contains two The example above is fine if the web page only con fine if the web page only contains twoThe example above is fine if the web page only contains twoThe example above is fine if the web page only contains two",
  },
  {
    id: 6,
    image:
      "https://image.shutterstock.com/image-photo/patan-ancient-city-kathmandu-valley-260nw-1137140381.jpg",
    name: "Bassi",
    Category: "NewDisk",
    description:
      "The example above is fine if the web page only contains two The example above is fine if the web page only contains twoThe example above is The example above is fine if the web page only contains two The example above is fine if the web page only con fine if the web page only contains twoThe example above is fine if the web page only contains twoThe example above is fine if the web page only contains two",
  },
  {
    id: 2,
    image:
      "https://www.shaadidukaan.com/vogue/wp-content/uploads/2019/08/hug-kiss-images.jpg",
    name: "Mr. Nobody",
    Category: "Random",
    description:
      "The example above is fine if the web page only contains two The example above is fine if the web page only contains twoThe example above is fine if the web page only contains twoThe example above is fine if the web page only contains twoThe example above is fine if the web page only contains two",
  },
];
export default Menu;